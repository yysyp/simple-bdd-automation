logging:
  level:
    root: INFO
    org.springframework.web: INFO

spring:
  application:
    name: simple-bdd-automation
  profiles:
    active: dev
  jackson:
    time-zone: GMT+8
    date-format: yyyy-MM-dd'T'HH:mm:ss.SSSZ


---

spring:
  config:
    activate:
      on-profile: dev




